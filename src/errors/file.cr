# Denotes an error that occurred while attempting to access a file.
class FileAccessError < Exception
end

# Denotes an error that occurred while attempting to create a file.
class FileCreateError < Exception
end

# Denotes an error that occurred while attempting to write to a file.
class FileWriteError  < Exception
end
