pub async fn assign(id: RoleId) -> Result<()> {
   return Ok(());
}


// IMPORTS ////////////////////////////////////////////////////////////////////////////////////////

use {
   serenity::{
      model::id::RoleId,
   },
};
